# Hostel Network Simulator

Download `chromedriver.exe` from `https://chromedriver.chromium.org/downloads `
In terminal

```
pip install -r requirements.txt
python app.py
```

## To Do

Keep a browser window open with the network conditions
